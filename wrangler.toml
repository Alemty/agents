name = "job-agent"
main = "src/index.ts"
compatibility_date = "2026-04-11"
account_id = "792fb5a1c2fb0af960074a1e869db0ed"

# Assets (frontend dashboard)
[assets]
directory = "./public"
not_found_handling = "none"

# D1 database (jobs, applications, stats)
# Migrations
[[migrations]]
tag = "v1"
new_directory = "migrations"

[[d1_databases]]
binding = "DB"
database_name = "jobs-db"
database_id = "849ffb99-99bd-427d-95d1-c84166738ac7"

# Environment variables
[vars]
MATCH_THRESHOLD = "60"
MAX_APPLICATIONS_DAY = "20"

# Secrets (set via wrangler secret put)
# LINKEDIN_EMAIL=
# LINKEDIN_PASSWORD=
# ANTHROPIC_API_KEY=

# Cron triggers — automatic job scraping
[triggers]
crons = ["0 10 * * 1", "0 14 * * 3"]  # Lunes 10am, Miércoles 2pm

# Routes
routes = [
  { pattern = "agents.alemty.eth.limo", custom_domain = true }
]
